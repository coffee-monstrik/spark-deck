
This is a log file to document how I set up this app:

I. Create a new project

```bash
npx create-next-app@latest
```
and I used recommended Next.js defaults

```bash
cd {project-name}
```

II. I create git page for my project with same {project-name}

```bash
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/coffee-monstrik/spark-deck.git
git push -u origin main
```

III.